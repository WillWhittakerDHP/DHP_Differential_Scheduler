# Session 6.12.3 Guide: Admin metadata — panels and `render_as`

**Purpose:** Tier harness session guide (task registered for `/task-start` / cascade). Work is retrospective.

**Tier:** Session (Tier 2)

---

## Quick Start

### Session Overview

**Session ID:** 6.12.3  
**Session Name:** Admin metadata — panels and `render_as`  
**Status:** Complete

### Tasks

- [x] #### Task 6.12.3.1: Panels, dispatcher, multiselect for `valid*` (retro)
**Goal:** Align persisted metadata with client routing and rendering (`fieldLocationDispatcher`, `collectionFieldKeys`, `computeRenderAs`, migrations).
**Files:** Dispatcher, `metadataRenderAsUtils`, `useEntityCardSubPanels`, `admin_metadata` migrations (see `sessions/task-6.12.3.1-planning.md`).
**Approach:** Delivered on branch; retro plan lists migration family `000028`–`000033`.
**Checkpoint:** `validParts` / `validAnnotations` / `validCascades` / `validEvents` multiselect; Events panel visibility.

---

## Session Workflow

For `/session-start`, checkpoints, and agent response norms, see `sessions/session-6.12.1-guide.md` (Guide Structure and Session Workflow).
