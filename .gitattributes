# Multi-machine: merge strategies for tracked harness state
# Append-only logs: keep lines from both sides
.project-manager/.audit-baseline-log.jsonl merge=union
# .tier-scope is gitignored (machine-local state); no merge strategy needed
