# Multi-machine: merge strategies for harness state (belt-and-suspenders if re-tracked)
# Append-only logs: keep lines from both sides
.project-manager/.audit-baseline-log.jsonl merge=union
# Machine-local scope: current branch wins
.project-manager/.tier-scope merge=ours
