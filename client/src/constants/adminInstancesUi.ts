/**
 * Tier-2 tab value under Instances: shape-definition panels (Part / Annotation / Event) live under this tab.
 * Reserved so useInstanceGrouping does not treat it as a missing blockShape id.
 */
export const INSTANCES_TIER2_SHAPES_DEFINITIONS_TAB = '__instances_tier2_shapes__' as const
