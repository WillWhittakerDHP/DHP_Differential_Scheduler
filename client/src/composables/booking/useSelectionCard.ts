/**
 * useSelectionCard (facade shim)
 *
 * WHY: Keep existing imports stable while the selection-card cluster lives under `booking/selectionCard/`.
 */

export * from './selectionCard/useSelectionCard'
