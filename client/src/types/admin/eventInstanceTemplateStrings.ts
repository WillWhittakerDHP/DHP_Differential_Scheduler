/**
 * WHY: Single structural base for event instance template fields (type-similarity / EXTEND).
 * Builder, preview draft slice, and create form all include these three strings.
 */
export interface EventInstanceTemplateStrings {
  titleTemplate: string
  descriptionTemplate: string
  locationTemplate: string
}
