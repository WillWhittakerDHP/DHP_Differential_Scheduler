**Instructions for AI / tooling:** Treat the findings in this report as canonical. Do not plan or change the audit scripts unless you have asked the user and received explicit approval to do so.

**When fixing a finding:** Search the codebase for the same rule or pattern (same ruleId or equivalent) and fix all similar occurrences consistently. Ensure the fix does not introduce new violations of this or related rules.

# Error Handling Audit (Generated)

This file is generated by `client/.scripts/error-handling-audit.mjs`.

Scope: `client/src` (ts, js, vue) and `server/src` (ts, mjs)

## Summary

- Files with findings: **0**
- Requiring review: **0**
- Allowed (with justification): 0

- P0 (silent catches): **0**
- P1 (console-in-catch, alert-in-catch, type suppressions): **0**
- P2 (general console, catch-without-logger): **0**

## Rules

- **empty-catch** (P0): Empty catch block
  - What: Empty catch block (no handling or logging).
  - Fix: Add createLogger and logger.error(err) at start of catch block, or rethrow.

- **silent-catch-promise** (P0): Silent .catch()
  - What: Promise .catch() with no-op or void callback.
  - Fix: Add error logging or pass error to a handler; avoid .catch(() => {}).

- **catch-comment-only** (P0): Catch block with only comment
  - What: Catch block whose only content is comments.
  - Fix: Add at least logger.error(err) or rethrow for visibility.

- **console-in-catch** (P1): console.* in catch block
  - What: console.log/warn/error/debug/info used inside a catch block.
  - Fix: Use createLogger and logger.error(err) instead of console in catch.

- **alert-in-catch** (P1): alert() in catch block
  - What: alert() used inside a catch block.
  - Fix: Use logger + notifyError (or app toast) instead of alert().

- **ts-ignore** (P1): @ts-ignore
  - What: TypeScript @ts-ignore comment suppressing type errors.
  - Fix: Fix the type or use @ts-expect-error with a short comment; prefer type-safe code.

- **ts-expect-error** (P1): @ts-expect-error
  - What: TypeScript @ts-expect-error comment.
  - Fix: Add brief comment explaining why; remove when upstream types are fixed.

- **as-any** (P1): as any
  - What: Type assertion "as any" bypassing type checking.
  - Fix: Use a proper type or type guard; avoid as any where possible.

- **eslint-disable** (P1): eslint-disable
  - What: ESLint disable comment suppressing lint rules.
  - Fix: Fix the underlying issue or scope the disable to the minimal line/block.

- **console-general** (P2): console.* usage
  - What: console.* usage outside catch (general logging).
  - Fix: Use createLogger and logger.debug/info/warn/error for app logging.

- **catch-without-logger** (P2): Catch block does not log error
  - What: Catch block that does not call logger.error/warn/info/debug.
  - Fix: Add logger.error(err) (or equivalent) at start of catch for ops visibility.

- **console-no-logger** (P2): console.* used without logger utility
  - What: File uses console.* but does not import createLogger.
  - Fix: Import createLogger and replace console calls with logger methods.

## Top hotspots (by score)

| File | Priority | Score | P0 | P1 | P2 |
| --- | --- | ---: | ---: | ---: | ---: |

## Per-file findings
