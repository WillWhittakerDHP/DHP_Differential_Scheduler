# Error Logging Audit (Generated)

This file is generated by `client/.scripts/error-logging-audit.mjs`.

Scope: `client/src` (ts, js, vue files) and `server/src` (ts, mjs files)

## Summary

- Files scanned: **588**
- Files with issues: **25**
- Issues found: **104**
- P0 (silent catches): 0
- P1 (console in catches): 0
- P2 (all console): 104

## Issues by File (sorted by priority)

### `server/src/services/googleCalendarService.ts` [P0] (score: 13)

- **INFO** (line 78): Found Console usage
  ```
  console.log(`[GoogleCalendarService] Cache hit for ${calendarEmails.length} calendars`);
  ```
  💡 Replace with logger for centralized log level control

- **INFO** (line 88): Found Console usage
  ```
  console.warn(`[GoogleCalendarService] Rate limit exceeded, waiting...`);
  ```
  💡 Replace with logger for centralized log level control

- **INFO** (line 105): Found Console usage
  ```
  console.log(`[GoogleCalendarService] Fetching free-busy for ${calendarEmails.length} calendars`);
  ```
  💡 Replace with logger for centralized log level control

- **INFO** (line 138): Found Console usage
  ```
  console.log(`[GoogleCalendarService] Successfully fetched free-busy data`);
  ```
  💡 Replace with logger for centralized log level control

- **INFO** (line 210): Found Console usage
  ```
  console.log(`[GoogleCalendarService] Events cache hit for ${calendarEmail}`);
  ```
  💡 Replace with logger for centralized log level control

- **INFO** (line 220): Found Console usage
  ```
  console.warn(`[GoogleCalendarService] Rate limit exceeded, waiting...`);
  ```
  💡 Replace with logger for centralized log level control

- **INFO** (line 230): Found Console usage
  ```
  console.log(`[GoogleCalendarService] Fetching events for ${calendarEmail}`);
  ```
  💡 Replace with logger for centralized log level control

- **INFO** (line 311): Found Console usage
  ```
  console.log(`[GoogleCalendarService] Successfully fetched ${events.length} events (with placeIds)`);
  ```
  💡 Replace with logger for centralized log level control

- **INFO** (line 441): Found Console usage
  ```
  console.warn('[GoogleCalendarService] Rate limit exceeded, waiting...');
  ```
  💡 Replace with logger for centralized log level control

- **INFO** (line 486): Found Console usage
  ```
  console.log(`[GoogleCalendarService] Creating event on calendar: ${calendarId}`);
  ```
  💡 Replace with logger for centralized log level control

- **INFO** (line 487): Found Console usage
  ```
  console.log(`[GoogleCalendarService] Event: "${summary}" from ${startDate.toISOString()} to ${endDat...
  ```
  💡 Replace with logger for centralized log level control

- **INFO** (line 504): Found Console usage
  ```
  console.log(`[GoogleCalendarService] Invalidating caches for calendar: ${calendarId}`);
  ```
  💡 Replace with logger for centralized log level control

- **INFO** (line 512): Found Console usage
  ```
  console.log(`[GoogleCalendarService] Successfully created event: ${createdEvent.id}`);
  ```
  💡 Replace with logger for centralized log level control

### `server/src/app.ts` [P0] (score: 12)

- **INFO** (line 16): Found Console usage
  ```
  console.log("✅ Database initialized successfully");
  ```
  💡 Replace with logger for centralized log level control

- **INFO** (line 46): Found Console usage
  ```
  console.log('[OAuthCallback] Callback route hit');
  ```
  💡 Replace with logger for centralized log level control

- **INFO** (line 47): Found Console usage
  ```
  console.log('[OAuthCallback] Query params:', JSON.stringify(req.query));
  ```
  💡 Replace with logger for centralized log level control

- **INFO** (line 48): Found Console usage
  ```
  console.log('[OAuthCallback] Full URL:', req.url);
  ```
  💡 Replace with logger for centralized log level control

- **INFO** (line 49): Found Console usage
  ```
  console.log('[OAuthCallback] Request headers:', JSON.stringify(req.headers));
  ```
  💡 Replace with logger for centralized log level control

- **INFO** (line 50): Found Console usage
  ```
  console.log('[OAuthCallback] Raw query string:', req.url.split('?')[1] || 'none');
  ```
  💡 Replace with logger for centralized log level control

- **INFO** (line 69): Found Console usage
  ```
  console.warn('[OAuthCallback] No authorization code received');
  ```
  💡 Replace with logger for centralized log level control

- **INFO** (line 70): Found Console usage
  ```
  console.log('[OAuthCallback] Query keys:', Object.keys(req.query));
  ```
  💡 Replace with logger for centralized log level control

- **INFO** (line 79): Found Console usage
  ```
  console.log('[OAuthCallback] Authorization code received, exchanging for tokens...');
  ```
  💡 Replace with logger for centralized log level control

- **INFO** (line 91): Found Console usage
  ```
  console.log('[OAuthCallback] OAuth authentication successful');
  ```
  💡 Replace with logger for centralized log level control

- **INFO** (line 92): Found Console usage
  ```
  console.log('[OAuthCallback] Has access token:', !!tokens.access_token);
  ```
  💡 Replace with logger for centralized log level control

- **INFO** (line 93): Found Console usage
  ```
  console.log('[OAuthCallback] Has refresh token:', !!tokens.refresh_token);
  ```
  💡 Replace with logger for centralized log level control

### `server/src/config/googleOAuth.ts` [P1] (score: 9)

- **INFO** (line 64): Found Console usage
  ```
  console.log('[GoogleOAuth] Client ID:', GOOGLE_CLIENT_ID);
  ```
  💡 Replace with logger for centralized log level control

- **INFO** (line 65): Found Console usage
  ```
  console.log('[GoogleOAuth] Redirect URI configured:', GOOGLE_REDIRECT_URI);
  ```
  💡 Replace with logger for centralized log level control

- **INFO** (line 66): Found Console usage
  ```
  console.log('[GoogleOAuth] Scopes:', GOOGLE_SCOPES);
  ```
  💡 Replace with logger for centralized log level control

- **INFO** (line 77): Found Console usage
  ```
  console.log('[GoogleOAuth] Redirect URI in auth URL:', redirectUriParam);
  ```
  💡 Replace with logger for centralized log level control

- **INFO** (line 78): Found Console usage
  ```
  console.log('[GoogleOAuth] Full generated auth URL:', authUrl);
  ```
  💡 Replace with logger for centralized log level control

- **INFO** (line 161): Found Console usage
  ```
  console.log('[GoogleOAuth] Tokens saved to file:', TOKEN_FILE);
  ```
  💡 Replace with logger for centralized log level control

- **INFO** (line 197): Found Console usage
  ```
  console.log('[GoogleOAuth] Tokens loaded from file');
  ```
  💡 Replace with logger for centralized log level control

- **INFO** (line 198): Found Console usage
  ```
  console.log('[GoogleOAuth] Has access token:', !!tokens.access_token);
  ```
  💡 Replace with logger for centralized log level control

- **INFO** (line 199): Found Console usage
  ```
  console.log('[GoogleOAuth] Has refresh token:', !!tokens.refresh_token);
  ```
  💡 Replace with logger for centralized log level control

### `server/src/routes/internal/appointments/appointmentRouter.ts` [P1] (score: 9)

- **INFO** (line 42): Found Console usage
  ```
  console.log('[AppointmentRouter] No availability_settings found, using default calendar');
  ```
  💡 Replace with logger for centralized log level control

- **INFO** (line 60): Found Console usage
  ```
  console.log('[AppointmentRouter] Calendar integration not enabled');
  ```
  💡 Replace with logger for centralized log level control

- **INFO** (line 65): Found Console usage
  ```
  console.error('[AppointmentRouter] Invalid calendar config: calendars must be an array');
  ```
  💡 Replace with logger for centralized log level control

- **INFO** (line 75): Found Console usage
  ```
  console.log(`[AppointmentRouter] Found writeTo calendar: ${writeToEntry.email}`);
  ```
  💡 Replace with logger for centralized log level control

- **INFO** (line 79): Found Console usage
  ```
  console.log('[AppointmentRouter] No writeTo calendar configured');
  ```
  💡 Replace with logger for centralized log level control

- **INFO** (line 255): Found Console usage
  ```
  console.log(`[AppointmentRouter] Created attendee records for appointment ${appointment.id}`);
  ```
  💡 Replace with logger for centralized log level control

- **INFO** (line 257): Found Console usage
  ```
  console.log(`[AppointmentRouter] No attendees provided for appointment ${appointment.id}`);
  ```
  💡 Replace with logger for centralized log level control

- **INFO** (line 265): Found Console usage
  ```
  console.log(`[AppointmentRouter] Creating calendar event for appointment ${appointment.id}`);
  ```
  💡 Replace with logger for centralized log level control

- **INFO** (line 290): Found Console usage
  ```
  console.log(`[AppointmentRouter] Skipping calendar event - status is '${appointment.status}' (not su...
  ```
  💡 Replace with logger for centralized log level control

### `server/src/services/appointmentCalendarService.ts` [P1] (score: 9)

- **INFO** (line 84): Found Console usage
  ```
  console.log(`[AppointmentCalendarService] Creating calendar event for appointment ${appointmentId}`)...
  ```
  💡 Replace with logger for centralized log level control

- **INFO** (line 139): Found Console usage
  ```
  console.log(`[AppointmentCalendarService] Created event ${createdEvent.id}, updated ${attendeesUpdat...
  ```
  💡 Replace with logger for centralized log level control

- **INFO** (line 260): Found Console usage
  ```
  console.error(`[AppointmentCalendarService] No time slots found for appointment ${appointment.id}`);
  ```
  💡 Replace with logger for centralized log level control

- **INFO** (line 261): Found Console usage
  ```
  console.error(`[AppointmentCalendarService] selectedTimeSlots:`, JSON.stringify(appointment.selected...
  ```
  💡 Replace with logger for centralized log level control

- **INFO** (line 266): Found Console usage
  ```
  console.error(`[AppointmentCalendarService] Time slot missing startTime/endTime for appointment ${ap...
  ```
  💡 Replace with logger for centralized log level control

- **INFO** (line 267): Found Console usage
  ```
  console.error(`[AppointmentCalendarService] firstSlot:`, JSON.stringify(firstSlot));
  ```
  💡 Replace with logger for centralized log level control

- **INFO** (line 275): Found Console usage
  ```
  console.log(`[AppointmentCalendarService] Using RFC3339 format: start=${firstSlot.startTime}, end=${...
  ```
  💡 Replace with logger for centralized log level control

- **INFO** (line 282): Found Console usage
  ```
  console.error(`[AppointmentCalendarService] Invalid RFC3339 dates: start=${firstSlot.startTime}, end...
  ```
  💡 Replace with logger for centralized log level control

- **INFO** (line 357): Found Console usage
  ```
  console.log(`[AppointmentCalendarService] Status sync not yet implemented for ${appointmentId}`);
  ```
  💡 Replace with logger for centralized log level control

### `server/src/config/app.ts` [P1] (score: 6)

- **INFO** (line 12): Found Console usage
  ```
  console.warn(`⚠️  ${envFile} not found, falling back to .env.development`);
  ```
  💡 Replace with logger for centralized log level control

- **INFO** (line 31): Found Console usage
  ```
  console.error("❌ Missing property in config:", error.message);
  ```
  💡 Replace with logger for centralized log level control

- **INFO** (line 32): Found Console usage
  ```
  console.error("🟠 Current env variables:", process.env);
  ```
  💡 Replace with logger for centralized log level control

- **INFO** (line 59): Found Console usage
  ```
  console.log("📦 Connecting to Database...");
  ```
  💡 Replace with logger for centralized log level control

- **INFO** (line 61): Found Console usage
  ```
  console.log("✅ Database connection established.");
  ```
  💡 Replace with logger for centralized log level control

- **INFO** (line 62): Found Console usage
  ```
  console.log("ℹ️  Run 'npm run migrate' to apply database migrations.");
  ```
  💡 Replace with logger for centralized log level control

### `client/src/utils/transformers/appointmentToWizardTransformer.ts` [P1] (score: 5)

- **INFO** (line 267): Found Console usage
  ```
  console.warn(`[AppointmentTransformer] userTypeId ${userTypeId} resolved to block instance but is no...
  ```
  💡 Replace with logger for centralized log level control

- **INFO** (line 269): Found Console usage
  ```
  console.warn(`[AppointmentTransformer] userTypeId ${userTypeId} not found in booking data`)
  ```
  💡 Replace with logger for centralized log level control

- **INFO** (line 339): Found Console usage
  ```
  console.warn(`[AppointmentTransformer] Some property type block IDs not found: ${missingIds.join(', ...
  ```
  💡 Replace with logger for centralized log level control

- **INFO** (line 363): Found Console usage
  ```
  console.warn(`[AppointmentTransformer] Some availability option IDs not found: ${missingIds.join(', ...
  ```
  💡 Replace with logger for centralized log level control

- **INFO** (line 387): Found Console usage
  ```
  console.warn(`[AppointmentTransformer] Some line item IDs not found: ${missingIds.join(', ')}`)
  ```
  💡 Replace with logger for centralized log level control

### `server/src/routes/internal/admin-metadata/adminMetadataRouter.ts` [P2] (score: 4)

- **INFO** (line 29): Found Console usage
  ```
  console.log('[AdminMetadataRouter] GET /admin-metadata/batch');
  ```
  💡 Replace with logger for centralized log level control

- **INFO** (line 92): Found Console usage
  ```
  console.log(`[AdminMetadataRouter] Batch returning: global counts = blockShape:${Object.keys(result....
  ```
  💡 Replace with logger for centralized log level control

- **INFO** (line 128): Found Console usage
  ```
  console.log(`[AdminMetadataRouter] Returning ${Object.keys(metadataRecord).length} metadata entries`...
  ```
  💡 Replace with logger for centralized log level control

- **INFO** (line 228): Found Console usage
  ```
  console.log(`[AdminMetadataRouter] POST /admin-metadata/${entityType}/${entityId}`, {
  ```
  💡 Replace with logger for centralized log level control

### `server/src/routes/internal/relationships/relationshipRouter.ts` [P2] (score: 4)

- **INFO** (line 35): Found Console usage
  ```
  console.error('[RelationshipRouter] Missing models:', {
  ```
  💡 Replace with logger for centralized log level control

- **INFO** (line 269): Found Console usage
  ```
  console.error('[RelationshipRouter] Model is undefined for:', req.params.relationshipType);
  ```
  💡 Replace with logger for centralized log level control

- **INFO** (line 296): Found Console usage
  ```
  console.warn(`[RelationshipRouter] Invalid parent_id format: ${parent_id}. Expected UUID, ignoring f...
  ```
  💡 Replace with logger for centralized log level control

- **INFO** (line 309): Found Console usage
  ```
  console.warn(`[RelationshipRouter] Invalid blockInstanceId format: ${blockInstanceId}. Expected UUID...
  ```
  💡 Replace with logger for centralized log level control

### `server/src/services/calendarErrorHandler.ts` [P2] (score: 4)

- **INFO** (line 244): Found Console usage
  ```
  console.warn(
  ```
  💡 Replace with logger for centralized log level control

- **INFO** (line 307): Found Console usage
  ```
  console.warn('[CalendarErrorHandler] No cache available, returning default value');
  ```
  💡 Replace with logger for centralized log level control

- **INFO** (line 326): Found Console usage
  ```
  console.error(`[${context}] Calendar API Error:`, {
  ```
  💡 Replace with logger for centralized log level control

- **INFO** (line 335): Found Console usage
  ```
  console.error(`[${context}] Original error:`, error.originalError);
  ```
  💡 Replace with logger for centralized log level control

### `server/src/utils/userTypeMapping.ts` [P2] (score: 4)

- **INFO** (line 57): Found Console usage
  ```
  console.warn(`[UserTypeMapping] No mapping found for role: ${role}`);
  ```
  💡 Replace with logger for centralized log level control

- **INFO** (line 84): Found Console usage
  ```
  console.warn(`[UserTypeMapping] UserTypeBlock not found for name: ${blockName}`);
  ```
  💡 Replace with logger for centralized log level control

- **INFO** (line 132): Found Console usage
  ```
  console.warn('[UserTypeMapping] No state control BlockShapes found');
  ```
  💡 Replace with logger for centralized log level control

- **INFO** (line 199): Found Console usage
  ```
  console.log(`[UserTypeMapping] Cache refreshed with ${userTypeBlocks.length} UserTypeBlocks`);
  ```
  💡 Replace with logger for centralized log level control

### `client/src/composables/booking/useWizardFilteredOptions.ts` [P2] (score: 3)

- **INFO** (line 189): Found Console usage
  ```
  console.warn('[useWizardFilteredOptions] Option block shape (type="option") not found')
  ```
  💡 Replace with logger for centralized log level control

- **INFO** (line 195): Found Console usage
  ```
  console.warn('[useWizardFilteredOptions] No cascade results from selected services. Falling back to ...
  ```
  💡 Replace with logger for centralized log level control

- **INFO** (line 208): Found Console usage
  ```
  console.warn('[useWizardFilteredOptions] Cascade results filtered out - no Option blocks found in ca...
  ```
  💡 Replace with logger for centralized log level control

### `server/src/index.ts` [P2] (score: 3)

- **INFO** (line 12): Found Console usage
  ```
  console.log(`🚀 Server running on port ${PORT}`);
  ```
  💡 Replace with logger for centralized log level control

- **INFO** (line 13): Found Console usage
  ```
  console.log(`\n${url}`);
  ```
  💡 Replace with logger for centralized log level control

- **INFO** (line 14): Found Console usage
  ```
  console.log(`${apiUrl}\n`);
  ```
  💡 Replace with logger for centralized log level control

### `server/src/services/driveTimeCache.ts` [P2] (score: 3)

- **INFO** (line 146): Found Console usage
  ```
  console.log(`[DriveTimeCache] Cache hit for ${key.substring(0, 50)}...`);
  ```
  💡 Replace with logger for centralized log level control

- **INFO** (line 175): Found Console usage
  ```
  console.log(`[DriveTimeCache] Cached drive time for ${key.substring(0, 50)}...`);
  ```
  💡 Replace with logger for centralized log level control

- **INFO** (line 190): Found Console usage
  ```
  console.log('[DriveTimeCache] Cache cleared');
  ```
  💡 Replace with logger for centralized log level control

### `client/src/composables/formFields/useFormFieldsContext.ts` [P2] (score: 2)

- **INFO** (line 103): Found Console usage
  ```
  console.warn(`[useFormFieldsContext] ${warningMessage}`)
  ```
  💡 Replace with logger for centralized log level control

- **INFO** (line 177): Found Console usage
  ```
  console.warn(`[useFormFieldsContext] ${warningMessage}`)
  ```
  💡 Replace with logger for centralized log level control

### `server/src/config/entityRegistry.ts` [P2] (score: 2)

- **INFO** (line 114): Found Console usage
  ```
  console.warn('[EntityRegistry] Models not yet initialized:', missingModels);
  ```
  💡 Replace with logger for centralized log level control

- **INFO** (line 115): Found Console usage
  ```
  console.warn('[EntityRegistry] This is normal during module loading - models will be available after...
  ```
  💡 Replace with logger for centralized log level control

### `server/src/routes/external/googleFetchRoutes.ts` [P2] (score: 2)

- **INFO** (line 35): Found Console usage
  ```
  console.error(
  ```
  💡 Replace with logger for centralized log level control

- **INFO** (line 56): Found Console usage
  ```
  console.error(
  ```
  💡 Replace with logger for centralized log level control

### `server/src/routes/internal/entities/entityRouter.ts` [P2] (score: 2)

- **INFO** (line 200): Found Console usage
  ```
  console.error('[EntityRouter] Error:', error);
  ```
  💡 Replace with logger for centralized log level control

- **INFO** (line 555): Found Console usage
  ```
  console.error('[EntityRouter] PATCH Error:', error);
  ```
  💡 Replace with logger for centralized log level control

### `server/src/services/appointmentSnapshotLoader.ts` [P2] (score: 2)

- **INFO** (line 59): Found Console usage
  ```
  console.warn(`[SnapshotLoader] Version ${versionId} not found`);
  ```
  💡 Replace with logger for centralized log level control

- **INFO** (line 80): Found Console usage
  ```
  console.warn(
  ```
  💡 Replace with logger for centralized log level control

### `client/src/composables/booking/useFreeBusyDataSource.ts` [P2] (score: 1)

- **INFO** (line 79): Found Console usage
  ```
  console.log('[useFreeBusyDataSource] Loading calendar config...')
  ```
  💡 Replace with logger for centralized log level control

### `client/src/composables/entityCrud/usePrimitiveMutation.ts` [P2] (score: 1)

- **INFO** (line 134): Found Console usage
  ```
  console.warn(`[usePrimitiveMutation] Field count mismatch - fields lost:`, {
  ```
  💡 Replace with logger for centralized log level control

### `server/src/routes/external/googleOauthRoutes.ts` [P2] (score: 1)

- **INFO** (line 75): Found Console usage
  ```
  console.log('[GoogleOAuthRoutes] OAuth authentication successful');
  ```
  💡 Replace with logger for centralized log level control

### `server/src/routes/helpers/dataController.ts` [P2] (score: 1)

- **INFO** (line 77): Found Console usage
  ```
  console.warn(
  ```
  💡 Replace with logger for centralized log level control

### `server/src/routes/internal/properties/propertyRouter.ts` [P2] (score: 1)

- **INFO** (line 433): Found Console usage
  ```
  console.error('[PropertyRouter] Error adding property type:', error);
  ```
  💡 Replace with logger for centralized log level control

### `server/src/test/setup/seedTestData.ts` [P2] (score: 1)

- **INFO** (line 135): Found Console usage
  ```
  console.warn('⚠️  No property versions found. Skipping appointment seeding.')
  ```
  💡 Replace with logger for centralized log level control
