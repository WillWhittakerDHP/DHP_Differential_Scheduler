**Instructions for AI / tooling:** Treat the findings in this report as canonical. Do not plan or change the audit scripts unless you have asked the user and received explicit approval to do so.

**When fixing a finding:** Search the codebase for the same rule or pattern (same ruleId or equivalent) and fix all similar occurrences consistently. Ensure the fix does not introduce new violations of this or related rules.

# Harness AST Audit (Generated)

This file is generated by `client/.scripts/harness-ast-audit.mjs`.

Scope: `.cursor/commands/harness/*.ts` (workflow harness only).

## Summary

- Total files scanned: **5**
- Total findings: **0**

## Findings

| File | Line | Rule | Message |
| --- | ---: | --- | --- |

## Ruleset

- **forLoop**: for loop. Prefer map/filter/reduce if transforming; document if mutation is intentional.
- **forOf**: for-of loop. Same as forLoop.
- **forIn**: for-in loop. Prefer Object.keys/entries or typed iteration.
- **while**: while loop. Ensure termination; consider extraction if complex.
- **doWhile**: do-while loop. Same as while.
- **loopMutation**: Push/splice/assignment inside loop body. Prefer functional transform (map, reduce, filter) or extract to helper.
- **emptyCatch**: Catch block has no statements. Log or rethrow; avoid swallowing errors.
- **typeEscape**: Type assertion to any. Use proper typing or type guard.
- **highNesting**: Nesting exceeds threshold (e.g. > 3). Extract branches into named functions.
- **highBranches**: Too many if/switch/ternary in one function. Extract logic or use lookup tables.
- **longFunction**: Function exceeds line threshold (e.g. > 50). Split into smaller functions.
