**Instructions for AI / tooling:** Treat the findings in this report as canonical. Do not plan or change the audit scripts unless you have asked the user and received explicit approval to do so.

**When fixing a finding:** Search the codebase for the same rule or pattern (same ruleId or equivalent) and fix all similar occurrences consistently. Ensure the fix does not introduce new violations of this or related rules.

# Deprecation & Legacy Accommodation Audit (Generated)

This file is generated by `client/.scripts/deprecation-audit.mjs`.

Scope: `client/src` (ts, js, vue) and `server/src` (ts, mjs)

## CRITICAL: Legacy and backward compatibility are NEVER permissible

**Findings in this report (deprecation, legacy markers, backward-compatibility accommodation) are NEVER permissible. They are HIGH refactor targets. Do not allowlist them in config; the goal is to eliminate these patterns or migrate off the legacy path. Scores are weighted so that legacy/compat findings produce high numeric values and high priority (P0/P1).**

## Purpose

Identifies deprecated code and runtime legacy accommodation patterns:
- **Annotated Deprecations**: `@deprecated`, `// Deprecated`, `(deprecated)`, `// LEGACY:`, compat markers
- **Runtime Legacy Accommodation**: `legacy`/`compat`/`fallback` keywords in code, `|| ''`, `?? ''`, default params

## Summary

**Legacy and backward-compatibility findings are NEVER permissible and are HIGH refactor targets.** Tier 1 (annotations, legacy-keyword, unhelpful-default-*) drives score and requiring-review count; legacy/compat rules contribute high numeric scores. Tier 2 (fallback-keyword, default-param, chaining-fallback) is report-only.

- Files with findings: **5**
- Requiring review (Tier 1): **5**
- Allowed (with justification): 0
- Annotated deprecations: **0**
- Runtime legacy accommodation: **5**
- Tier 1 findings: 5 (annotations: 0, legacy-keyword: 0, unhelpful-default: 5)

## Rules

- **jsdoc-deprecated** (warning): JSDoc @deprecated
  - What: JSDoc @deprecated tag present.
  - Fix: Document replacement or remove when no longer used; add migration note if needed.

- **comment-deprecated** (warning): Deprecated comment
  - What: Line comment containing "deprecated" or "DEPRECATED".
  - Fix: Add replacement or remove; use @deprecated JSDoc where applicable.

- **paren-deprecated** (warning): Parenthetical deprecation note
  - What: Parenthetical (deprecated ...) note in code.
  - Fix: Replace with supported API or document migration path.

- **block-deprecated** (warning): Block comment deprecation
  - What: Block comment containing deprecation.
  - Fix: Document replacement and timeline for removal.

- **legacy-marker** (info): Legacy marker comment
  - What: // LEGACY: or // legacy comment.
  - Fix: Plan removal or document why legacy path is still needed.

- **compat-marker** (info): Migration compatibility marker
  - What: "kept for migration/backward compatibility" phrase.
  - Fix: Add ticket or timeline to remove when migration is complete.

- **legacy-keyword** (warning): Legacy keyword in runtime code
  - What: Runtime code uses legacy/compat/backward/workaround/shim/polyfill.
  - Fix: Refactor to supported path or document and scope usage.

- **fallback-keyword** (info): Fallback keyword in runtime code
  - What: Runtime code uses "fallback" keyword.
  - Fix: Ensure fallback is intentional and logged if it masks errors.

- **unhelpful-default-or** (warning): Unhelpful default (|| empty)
  - What: || '', || [], || {} used as default.
  - Fix: Prefer explicit handling or ?? for null/undefined; avoid masking errors.

- **unhelpful-default-nullish** (warning): Unhelpful default (?? empty)
  - What: ?? '', ?? [], ?? {} used as default.
  - Fix: Use when intentional; avoid silencing null/undefined from APIs.

- **default-param** (info): Default function parameter
  - What: Default function parameter with string/array/object literal.
  - Fix: Ensure default is intentional and documented.

- **chaining-fallback** (info): Optional chaining with default
  - What: Optional chaining (?.) with ?? or || default.
  - Fix: Ensure default is correct and does not hide missing data.

## Top hotspots (by score)

| File | Priority | Score | Annotations | Legacy/Compat |
| --- | --- | ---: | ---: | ---: |
| `client/src/components/booking/steps/AvailabilityStep.vue` | P2 | 2 | 0 | 1 |
| `client/src/composables/booking/useAvailabilityDefaults.ts` | P2 | 2 | 0 | 1 |
| `client/src/composables/useThemeMode.ts` | P2 | 2 | 0 | 1 |
| `client/src/utils/booking/buildClientLinks.ts` | P2 | 2 | 0 | 1 |
| `server/src/services/invites/inviteContextBuilder.ts` | P2 | 2 | 0 | 1 |

## Per-file findings

### `client/src/components/booking/steps/AvailabilityStep.vue` [P2] (score: 2)

```
[LEGACY] unhelpful-default-nullish@174: return o.emptyStateMessage.value ?? ''
```

### `client/src/composables/booking/useAvailabilityDefaults.ts` [P2] (score: 2)

```
[LEGACY] unhelpful-default-nullish@78: const slotList = (slots as TimeSlot[] | null) ?? []
```

### `client/src/composables/useThemeMode.ts` [P2] (score: 2)

```
[LEGACY] unhelpful-default-nullish@18: /** Explicit fallbacks to satisfy deprecation audit (avoid ?? '', ?? {} in-place). */
```

### `client/src/utils/booking/buildClientLinks.ts` [P2] (score: 2)

```
[LEGACY] unhelpful-default-nullish@17: return import.meta.env?.VITE_APP_BASE_URL ?? ''
```

### `server/src/services/invites/inviteContextBuilder.ts` [P2] (score: 2)

```
[LEGACY] unhelpful-default-nullish@3: const base = process.env.APP_BASE_URL ?? process.env.VITE_APP_BASE_URL ?? ''
```

## Notes

- **[DEPR]**: Annotated deprecation markers in comments
- **[LEGACY]**: Runtime legacy accommodation patterns in code
- **Legacy and backward-compat findings are NEVER permissible.** Do not add config allowlist entries for them; refactor or migrate instead.
- **P0**: High refactor priority (cleanup soon); score threshold indicates high density or legacy/compat findings
- **P1**: High refactor priority (schedule cleanup)
- **P2**: Refactor when convenient; still not permissible to allowlist
