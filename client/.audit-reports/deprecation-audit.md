# Deprecation Audit (Generated)

This file is generated by `client/.scripts/deprecation-audit.mjs`.

Scope: `client/src` (ts, js, vue files) and `server/src` (ts, mjs files)

## Purpose

Identifies deprecated code that should be cleaned up, including:
- `@deprecated` JSDoc tags
- `// Deprecated` comments
- `(deprecated - use X)` inline notes
- Legacy and migration compatibility markers

## Summary

- Files scanned: **598**
- Files with deprecations: **25**
- Deprecation markers found: **32**
- With replacement suggestion: 0
- Without replacement: 32

## Needs Review (no replacement specified)

These deprecations need investigation to determine if they can be removed:

### `client/src/components/admin/generic/EntityFormContent.vue`

- **Line 29** (info): Found Migration compatibility
  ```
  * PATTERN: Keep rendering defaulted to true for backward compatibility
  ```

### `client/src/components/booking/types/selectionCardTypes.ts`

- **Line 66** (info): Found Migration compatibility
  ```
  * PATTERN: Comprehensive config object with defaults for backward compatibility
  ```

### `client/src/composables/admin/useShapeEditModal.ts`

- **Line 55** (info): Found Migration compatibility
  ```
  // Also collapse the card if it was expanded (for backward compatibility)
  ```

### `client/src/composables/admin/useStatusButtonToggle.ts`

- **Line 58** (info): Found Migration compatibility
  ```
  // If entity is provided, use it (for backward compatibility)
  ```

### `client/src/composables/booking/selectionCard/useSelectionCardConfig.ts`

- **Line 33** (info): Found Migration compatibility
  ```
  * LEARNING: Config with defaults for backward compatibility
  ```

### `client/src/composables/booking/selectionCard/useSelectionCardHandlers.ts`

- **Line 67** (info): Found Migration compatibility
  ```
  // Fallback to emit for backward compatibility
  ```

### `client/src/composables/booking/selectionCard/useSelectionCardState.ts`

- **Line 58** (info): Found Migration compatibility
  ```
  // Create local plugin for backward compatibility
  ```

- **Line 84** (info): Found Migration compatibility
  ```
  // Fallback to modelValue for backward compatibility
  ```

### `client/src/composables/booking/useAppointmentDuration.ts`

- **Line 75** (info): Found Migration compatibility
  ```
  // NOTE: Uses 'Major' as fallback for backward compatibility, but should use major event from availabilitySettings
  ```

### `client/src/composables/booking/useAppointmentTimes.ts`

- **Line 102** (info): Found Migration compatibility
  ```
  // NOTE: Uses 'Major'/'Minor' as fallback for backward compatibility
  ```

- **Line 156** (info): Found Migration compatibility
  ```
  // NOTE: Uses 'Major'/'Minor' as fallback for backward compatibility
  ```

### `client/src/composables/booking/useTimeSlotCalculations.ts`

- **Line 29** (info): Found Migration compatibility
  ```
  * NOTE: Property names 'major' and 'minor' kept for backward compatibility, but represent major/minor perspectives
  ```

### `client/src/composables/booking/useWizardFilteredOptions.ts`

- **Line 88** (info): Found Migration compatibility
  ```
  selectedServices: Ref<BookingBlockInstance[]> // Note: This param name kept for backward compatibility, but receives sel...
  ```

### `client/src/composables/useAdmin.ts`

- **Line 206** (info): Found Migration compatibility
  ```
  getGlobalEntities, // Keep for backward compatibility
  ```

- **Line 207** (info): Found Migration compatibility
  ```
  getGlobalEntityById, // Keep for backward compatibility
  ```

### `client/src/composables/useBookingWizard.ts`

- **Line 77** (info): Found Migration compatibility
  ```
  * PATTERN: Replace array with single selection (keeps array structure for backward compatibility)
  ```

### `client/src/composables/useRelationship.ts`

- **Line 76** (info): Found Migration compatibility
  ```
  // Transform GlobalRelationship[] to FetchedRelationship[] for backward compatibility
  ```

- **Line 80** (info): Found Migration compatibility
  ```
  // For backward compatibility, provide isLoading and error (always false/undefined since we're reading from cache)
  ```

### `client/src/types/annotations.ts`

- **Line 27** (info): Found Migration compatibility
  ```
  * For backward compatibility, "text" is included and equals "name"
  ```

### `client/src/types/property.ts`

- **Line 24** (info): Found Migration compatibility
  ```
  id: string; // PropertyVersion ID (for backward compatibility)
  ```

### `client/src/utils/booking/appointmentSlotBuilder.ts`

- **Line 142** (info): Found Migration compatibility
  ```
  * @param validPartsRelationships - Array of validParts relationships (for backward compatibility, not used in new logic)
  ```

### `client/src/utils/differentialScheduling.ts`

- **Line 10** (info): Found Migration compatibility
  ```
  * Defaults to major and minor for backward compatibility
  ```

### `client/src/utils/iconMapper.ts`

- **Line 12** (info): Found Migration compatibility
  ```
  for backward compatibility and admin-entered values
  ```

### `client/src/utils/transformers/annotationTransformers.ts`

- **Line 69** (info): Found Migration compatibility
  ```
  // Note: userTypeBlock on AnnotationInstance entity is deprecated, but we keep it for backward compatibility
  ```

- **Line 79** (info): Found Migration compatibility
  ```
  ?? rawAnnotation.annotationShape ?? rawAnnotation.annotation_type ?? rawAnnotation.AnnotationShape // Fallback for backw...
  ```

### `client/src/utils/transformers/appointmentToWizardTransformer.ts`

- **Line 193** (info): Found Migration compatibility
  ```
  // LEARNING: Convert boolean to TernaryBoolean for backward compatibility
  ```

### `client/src/utils/transformers/fetchToGlobalTransformer.ts`

- **Line 238** (info): Found Migration compatibility
  ```
  // Attach instanceComponents arrays to entities (for backward compatibility)
  ```

### `client/src/utils/transformers/globalToBookingTransformer.ts`

- **Line 49** (info): Found Migration compatibility
  ```
  blockShape: string // Denormalized: blockShape name instead of ID (kept for backward compatibility)
  ```

- **Line 302** (info): Found Migration compatibility
  ```
  // LEARNING: Convert boolean to TernaryBoolean for backward compatibility during migration
  ```

- **Line 321** (info): Found Migration compatibility
  ```
  blockShape, // Keep for backward compatibility
  ```

### `server/src/db/models/booking/annotation_instance.ts`

- **Line 31** (info): Found Migration compatibility
  ```
  * NOTE: The userType field on this model is kept for backward compatibility but is being
  ```

### `server/src/routes/internal/index.ts`

- **Line 30** (info): Found Migration compatibility
  ```
  // NOTE: Keeping /admin-input-metadata and /admin-primitive-metadata paths for backward compatibility during transition
  ```

## Files by Priority

| File | Priority | Score | Deprecations | With Replacement |
| --- | --- | ---: | ---: | ---: |
| `client/src/utils/transformers/globalToBookingTransformer.ts` | P2 | 3 | 3 | 0 |
| `client/src/composables/booking/selectionCard/useSelectionCardState.ts` | P2 | 2 | 2 | 0 |
| `client/src/composables/booking/useAppointmentTimes.ts` | P2 | 2 | 2 | 0 |
| `client/src/composables/useAdmin.ts` | P2 | 2 | 2 | 0 |
| `client/src/composables/useRelationship.ts` | P2 | 2 | 2 | 0 |
| `client/src/utils/transformers/annotationTransformers.ts` | P2 | 2 | 2 | 0 |
| `client/src/components/admin/generic/EntityFormContent.vue` | P2 | 1 | 1 | 0 |
| `client/src/components/booking/types/selectionCardTypes.ts` | P2 | 1 | 1 | 0 |
| `client/src/composables/admin/useShapeEditModal.ts` | P2 | 1 | 1 | 0 |
| `client/src/composables/admin/useStatusButtonToggle.ts` | P2 | 1 | 1 | 0 |
| `client/src/composables/booking/selectionCard/useSelectionCardConfig.ts` | P2 | 1 | 1 | 0 |
| `client/src/composables/booking/selectionCard/useSelectionCardHandlers.ts` | P2 | 1 | 1 | 0 |
| `client/src/composables/booking/useAppointmentDuration.ts` | P2 | 1 | 1 | 0 |
| `client/src/composables/booking/useTimeSlotCalculations.ts` | P2 | 1 | 1 | 0 |
| `client/src/composables/booking/useWizardFilteredOptions.ts` | P2 | 1 | 1 | 0 |
| `client/src/composables/useBookingWizard.ts` | P2 | 1 | 1 | 0 |
| `client/src/types/annotations.ts` | P2 | 1 | 1 | 0 |
| `client/src/types/property.ts` | P2 | 1 | 1 | 0 |
| `client/src/utils/booking/appointmentSlotBuilder.ts` | P2 | 1 | 1 | 0 |
| `client/src/utils/differentialScheduling.ts` | P2 | 1 | 1 | 0 |
| `client/src/utils/iconMapper.ts` | P2 | 1 | 1 | 0 |
| `client/src/utils/transformers/appointmentToWizardTransformer.ts` | P2 | 1 | 1 | 0 |
| `client/src/utils/transformers/fetchToGlobalTransformer.ts` | P2 | 1 | 1 | 0 |
| `server/src/db/models/booking/annotation_instance.ts` | P2 | 1 | 1 | 0 |
| `server/src/routes/internal/index.ts` | P2 | 1 | 1 | 0 |

## Notes

- **P0**: High deprecation density (cleanup soon)
- **P1**: Moderate deprecations (schedule cleanup)
- **P2**: Low priority (cleanup when convenient)
