/** Select-option row shape shared by metadata entry assembly and input-config codec. */
export interface AdminSelectOptionRow {
  displayOrder: number
  label: string
  valuePayload: string | null
}
